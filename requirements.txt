# PLH Rack Monitor - backend runtime dependencies (Python 3.14.7)
# Versions below match what is installed and verified in .venv on the target host.
fastapi==0.141.1
uvicorn[standard]==0.53.0
psutil==7.2.2
httpx==0.28.1
python-dotenv==1.2.3
pydantic==2.13.5

# Test dependencies
pytest==9.1.1
pytest-asyncio==1.4.0
